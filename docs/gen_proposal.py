# -*- coding: utf-8 -*-
"""
Generate project proposal Word document for Minesweeper.
南通大学 C语言课程实践 项目计划书
Format: 宋体小四, 1.5倍行距, 首行缩进2字符, 页边距2cm
"""

from docx import Document
from docx.shared import Pt, Cm, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn
import os

doc = Document()

# ============================================================
# Page setup: margins 2cm, binding 0.5cm left
# ============================================================
for section in doc.sections:
    section.top_margin = Cm(2)
    section.bottom_margin = Cm(2)
    section.left_margin = Cm(2.5)  # 2cm + 0.5cm binding
    section.right_margin = Cm(2)

# ============================================================
# Style helpers
# ============================================================
def set_font(run, name_cn='宋体', name_en='Times New Roman', size=12, bold=False, color=None):
    run.font.size = Pt(size)
    run.font.name = name_en
    run.font.bold = bold
    r = run._element
    r.rPr.rFonts.set(qn('w:eastAsia'), name_cn)
    if color:
        run.font.color.rgb = color

def add_body(doc, text, indent=True):
    """Add a body paragraph with proper formatting."""
    p = doc.add_paragraph()
    pf = p.paragraph_format
    pf.line_spacing = 1.5
    pf.space_before = Pt(0)
    pf.space_after = Pt(0)
    if indent:
        pf.first_line_indent = Pt(24)  # ~2 Chinese chars
    run = p.add_run(text)
    set_font(run, size=12)  # 小四 = 12pt
    return p

def add_heading_ch1(doc, text):
    """Chapter heading: 三号黑体"""
    p = doc.add_paragraph()
    pf = p.paragraph_format
    pf.line_spacing = 1.5
    pf.space_before = Pt(12)
    pf.space_after = Pt(6)
    pf.alignment = WD_ALIGN_PARAGRAPH.LEFT
    run = p.add_run(text)
    set_font(run, name_cn='黑体', size=16, bold=True)
    return p

def add_heading_ch2(doc, text):
    """Section heading: 小三楷体"""
    p = doc.add_paragraph()
    pf = p.paragraph_format
    pf.line_spacing = 1.5
    pf.space_before = Pt(8)
    pf.space_after = Pt(4)
    run = p.add_run(text)
    set_font(run, name_cn='楷体', size=15, bold=True)
    return p

def add_heading_ch3(doc, text):
    """Subsection heading: 四号宋体粗体"""
    p = doc.add_paragraph()
    pf = p.paragraph_format
    pf.line_spacing = 1.5
    pf.space_before = Pt(6)
    pf.space_after = Pt(2)
    run = p.add_run(text)
    set_font(run, name_cn='宋体', size=14, bold=True)
    return p

# ============================================================
# Cover page
# ============================================================
for _ in range(3):
    doc.add_paragraph()

p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('C语言课程实践')
set_font(run, name_cn='黑体', size=22, bold=True)

p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('项 目 计 划 书')
set_font(run, name_cn='黑体', size=26, bold=True)

doc.add_paragraph()

p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('智能扫雷——基于Win32 GDI的多功能扫雷游戏')
set_font(run, name_cn='楷体', size=18, bold=True)

for _ in range(4):
    doc.add_paragraph()

info_items = [
    ('班    级', '________________'),
    ('组    号', '________________'),
    ('学生姓名', '________________'),
    ('指导教师', '________________'),
    ('完成日期', '________________'),
]
for label, blank in info_items:
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    run = p.add_run(f'{label}：{blank}')
    set_font(run, name_cn='宋体', size=14)

doc.add_paragraph()
p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('南通大学地理科学学院')
set_font(run, name_cn='宋体', size=14)
p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('二〇二五年  月')
set_font(run, name_cn='宋体', size=14)

doc.add_page_break()

# ============================================================
# Table of Contents (placeholder)
# ============================================================
p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('目   录')
set_font(run, name_cn='黑体', size=16, bold=True)

toc_items = [
    '第一章 项目介绍',
    '  1.1 项目背景与选题依据',
    '  1.2 项目目标',
    '  1.3 开发环境',
    '  1.4 交付成果',
    '第二章 系统设计与实现',
    '  2.1 总体架构设计',
    '  2.2 核心数据结构设计',
    '  2.3 核心算法设计',
    '  2.4 功能模块详细设计',
    '  2.5 界面设计',
    '  2.6 文件结构设计',
    '第三章 系统测试与验证',
    '  3.1 自动化单元测试',
    '  3.2 手动集成测试',
    '  3.3 性能指标',
    '第四章 项目管理与总结',
    '  4.1 团队分工',
    '  4.2 开发进度计划',
    '  4.3 技术难点与应对方案',
    '  4.4 项目特色与创新点总结',
    '参考资料',
]
for item in toc_items:
    p = doc.add_paragraph()
    run = p.add_run(item)
    set_font(run, size=12)
    p.paragraph_format.line_spacing = 1.5

doc.add_page_break()

# ============================================================
# Chapter 1
# ============================================================
add_heading_ch1(doc, '第一章 项目介绍')

add_heading_ch2(doc, '1.1 项目背景与选题依据')

add_body(doc, '扫雷是Windows操作系统中流传最广的单机益智游戏。1990年微软将其内置于Windows 3.1系统，此后三十余年间，扫雷以其规则简洁、逻辑深邃的特点吸引了全球数亿玩家。扫雷的核心玩法可以概括为一句话：在一片被地雷覆盖的方格区域中，玩家依靠每个格子显示的周围地雷数量，通过逻辑推理逐步揭开所有安全格子，最终找出并标记全部地雷。这一过程涉及的算法与数据结构——广度优先搜索（BFS）、约束传播、概率推断、图的遍历——恰好与C语言课程的核心教学内容高度吻合。')

add_body(doc, '本项目选择扫雷作为C语言课程实践的选题，基于三方面考量。第一，扫雷的核心算法覆盖面广：洪水填充需要BFS队列实现，无猜测雷区生成需要约束传播求解器，概率热力图需要组合枚举与采样算法。单一项目即可覆盖数据结构课程中队列、动态数组、位运算、图遍历等多个核心知识点。第二，Win32 GDI图形编程是C语言系统级开发的经典实践方向：通过调用Windows API实现窗口创建、消息循环、双缓冲渲染、菜单系统、定时器管理，学生能够深入理解操作系统提供给应用程序的底层接口。第三，在经典玩法基础上扩展创新功能（AI自动求解演示、概率X光透视、局域网双人竞速、拓扑环面模式等），能够充分展示团队在算法设计、软件架构与用户体验方面的综合能力。')

add_heading_ch2(doc, '1.2 项目目标')

add_body(doc, '本项目的总体目标是开发一款功能完备、界面精美、体验流畅的Windows桌面扫雷游戏，在忠实还原Windows 7经典扫雷视觉风格的基础上，实现五个层面的创新突破。')

add_body(doc, '算法创新层面，项目将实现基于约束传播的三规则迭代求解器。该求解器不仅能够验证雷区是否可解（支撑"无猜测模式"），还能逐步输出推理过程（支撑"AI自动演示"），并计算每个未揭开格子的精确地雷概率（支撑"概率热力图"）。一个算法引擎同时驱动三个用户可见的功能，体现了算法复用与架构设计的统一。')

add_body(doc, '玩法创新层面，项目将引入四种非传统游戏模式：战争迷雾模式限制玩家视野，环面模式将棋盘上下左右连通形成拓扑学中的甜甜圈结构，限时挑战模式增加倒计时紧张感，地雷密度调节允许用户按百分比自定义难度。')

add_body(doc, '交互创新层面，项目将实现翻格波纹动画（格子从点击点以BFS顺序逐个翻开而非瞬间全部出现）、四套可切换的颜色主题（经典灰、暗黑、海洋蓝、复古像素）、完整的键盘无障碍操作（方向键移动光标、空格揭开、Tab智能跳转）、踩雷时的窗口震动与地雷逐个显现特效，以及按住Alt键实时查看概率分布的"X光透视"功能。')

add_body(doc, '社交功能层面，项目将实现本地用户注册登录系统、个人成绩与排行榜持久化存储、游戏操作录制与回放播放，以及基于Winsock TCP协议的局域网双人同板竞速对战。')

add_body(doc, '工程目标层面，项目采用模块化架构（10个独立模块，各模块通过头文件接口解耦），配备自动化单元测试（10项测试用例），编译产物为单一可执行文件（不超过200KB，零外部依赖），可在任何Windows 7及以上系统上直接运行。')

add_heading_ch2(doc, '1.3 开发环境')

add_body(doc, '本项目使用C语言（C99标准）编写全部代码，编译器选用MinGW GCC 8.0以上版本。图形界面基于Windows原生的Win32 API（GDI子系统）实现，网络通信基于Windows原生的Winsock 2.2接口实现。目标平台为Windows 7、Windows 10和Windows 11操作系统。项目使用Git进行版本管理，代码托管于GitHub平台。程序运行所需的全部库（gdi32、winmm、comdlg32、ws2_32）均为Windows系统自带的动态链接库，无需安装任何第三方依赖。')

add_heading_ch2(doc, '1.4 交付成果')

add_body(doc, '本项目计划交付以下五项成果：可执行程序minesweeper.exe（单文件，零外部依赖，双击即可运行）；完整源代码（含详细注释，按include头文件、src源文件、test测试文件三级目录组织）；自动化测试程序test_core.exe（10项核心逻辑测试用例）；一键编译脚本build.bat（在任何安装了MinGW的Windows电脑上执行即可编译）；项目文档（本计划书、课程实践报告、PPT汇报演示文稿）。')

doc.add_page_break()

# ============================================================
# Chapter 2
# ============================================================
add_heading_ch1(doc, '第二章 系统设计与实现')

add_heading_ch2(doc, '2.1 总体架构设计')

add_body(doc, '系统采用模块化分层架构，将程序划分为10个功能独立的模块。每个模块由一对.h头文件和.c源文件组成，头文件定义公共接口（函数声明、数据结构），源文件包含具体实现。模块之间的依赖关系遵循单向原则：上层模块调用下层模块的接口，下层模块不依赖上层模块。这一设计使得每个模块可以独立编译、独立测试，也便于多人并行开发。')

add_body(doc, '系统的模块层次自底向上分为四层。最底层是数据层，由board模块构成，负责棋盘的内存管理、格子状态存储、布雷算法、BFS洪水填充、邻居计算（含环面拓扑支持）。第二层是逻辑层，包括game模块（游戏状态机、点击处理、动画队列管理）和solver模块（约束求解器、无猜测生成、概率计算、单步求解）。第三层是表现层，包括render模块（Win32 GDI双缓冲渲染、主题系统、3D边框绘制、热力图叠加、光标绘制）和sound模块（异步音效播放）。第四层是功能层，包括replay模块（操作录制与回放）、stats模块（排行榜与统计持久化）、user模块（用户注册登录）和net模块（Winsock TCP局域网对战）。最顶层是入口层，由main模块负责WinMain函数、消息循环、菜单创建、所有对话框、定时器调度以及各模块的集成调用。')

# Architecture diagram as table
table = doc.add_table(rows=5, cols=1)
table.style = 'Table Grid'
cells_text = [
    'main.c — 入口层（WinMain、消息循环、对话框、菜单、定时器调度）',
    'replay.c | stats.c | user.c | net.c — 功能层（回放、排行榜、用户、联机）',
    'render.c | sound.c — 表现层（GDI渲染、主题系统、音效）',
    'game.c | solver.c — 逻辑层（状态机、求解器、概率计算）',
    'board.c — 数据层（棋盘数据结构、布雷、BFS洪水填充、环面拓扑）',
]
for i, txt in enumerate(cells_text):
    cell = table.cell(i, 0)
    cell.text = ''
    p = cell.paragraphs[0]
    run = p.add_run(txt)
    set_font(run, size=10)
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER

p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = p.add_run('图2.1 系统模块分层架构')
set_font(run, size=10)

add_heading_ch2(doc, '2.2 核心数据结构设计')

add_body(doc, '棋盘的基本单元是格子（Cell），使用两个字节存储：一个字节用位掩码记录格子的四种状态（是否为地雷、是否已揭开、是否已插旗、是否标记问号），另一个字节记录该格子周围的地雷数量（0至8的整数）。这种紧凑的存储方式使得一个30×16的高级棋盘仅占用960字节内存。')

add_body(doc, '棋盘（Board）结构体包含宽度、高度、地雷总数、格子数组指针、已揭开格子计数、已插旗格子计数、游戏状态（就绪、进行中、胜利、失败四种状态）、首次点击标记和环面模式开关。格子数组采用一维动态数组（通过malloc分配），通过y×width+x的公式将二维坐标映射为一维索引。')

add_body(doc, '游戏上下文（Game）结构体是整个程序的核心状态容器，除了持有Board指针外，还包含当前难度设置、计时器秒数、无猜测模式开关、战争迷雾开关及可视半径、热力图开关、提示位置、九宫格按压状态、踩雷位置记录、动画队列（格子索引数组、队列长度、当前播放位置、动画类型）、键盘光标位置与闪烁计数器、AI自动演示状态与高亮位置，以及环面模式开关。')

add_body(doc, '颜色主题（ColorTheme）结构体包含18项颜色定义：背景色、未揭开格子色、已揭开格子色、按压格子色、四级3D边框色（白、浅灰、深灰、黑）、LED面板背景色与前景色、工具栏背景色与激活色、迷雾色、爆炸背景色、地雷颜色、旗帜颜色、键盘光标颜色，以及数字1至8的颜色数组。系统预定义四套主题实例，运行时通过索引切换。')

add_heading_ch2(doc, '2.3 核心算法设计')

add_heading_ch3(doc, '2.3.1 BFS洪水填充算法')

add_body(doc, '当玩家点击一个周围无雷的空格时，系统需要自动展开该格子所在的整个安全区域。本项目采用广度优先搜索实现该功能，具体步骤为：将起始格子入队，循环取出队首格子并揭开，若该格子的数字为0（周围无雷），则将其所有未揭开、未插旗的邻居格子入队。使用显式队列（动态分配的整型数组）而非递归调用，避免在大棋盘上因递归层数过深导致栈溢出。算法时间复杂度为O(n)，空间复杂度为O(n)，其中n为棋盘格子总数。')

add_body(doc, '为了支持翻格波纹动画，项目额外实现了一个非破坏性版本的BFS函数：该函数执行与洪水填充相同的BFS遍历逻辑，但不修改棋盘状态，而是将所有待揭开的格子按BFS访问顺序记录到一个输出数组中。随后，动画系统通过定时器每15毫秒揭开3至5个格子，实现从点击点向外扩散的波纹视觉效果。')

add_heading_ch3(doc, '2.3.2 约束求解器')

add_body(doc, '约束求解器是本项目的算法核心，承担三项职能：验证雷区可解性（无猜测模式）、提供单步安全提示（提示系统）、逐步展示推理过程（AI自动演示）。')

add_body(doc, '求解器迭代应用三条推理规则直到无法推进。规则一（安全推断）：若某个已揭开的数字格，其周围已插旗的格子数恰好等于该数字，则该数字格的其余未揭开邻居均为安全格，可以揭开。规则二（地雷推断）：若某个已揭开的数字格，其数字减去已插旗数等于未揭开邻居数，则这些未揭开邻居均为地雷，应当插旗。规则三（约束相减）：对于相邻的两个已揭开数字格A和B，若A的未揭开邻居集合是B的未揭开邻居集合的子集，则B的差集中包含的地雷数量等于B的剩余雷数减去A的剩余雷数。当该差值为0时，差集中的格子均安全；当该差值等于差集大小时，差集中的格子均为地雷。')

add_body(doc, '无猜测雷区生成的流程为：随机布雷后，在棋盘的副本上从玩家首次点击位置开始运行完整的求解器。若求解器能够揭开所有非雷格子，则该雷区通过验证；否则重新随机布雷并再次验证，最多尝试1000次。这一机制从数学上保证了每一局游戏都有纯逻辑解法，玩家无需依靠运气猜测。')

add_body(doc, 'AI自动演示功能依赖求解器的单步模式：每次调用只执行一步推理动作（揭开一个安全格或标记一个地雷），并返回触发该动作的推理依据格子坐标。主程序通过300毫秒间隔的定时器反复调用该函数，同时在界面上用蓝色边框高亮推理依据格子，使玩家能够直观地观看AI的推理过程。')

add_heading_ch3(doc, '2.3.3 概率热力图计算')

add_body(doc, '对于求解器三条规则均无法确定的格子，系统通过枚举或采样方法计算其地雷概率。首先识别边界格子（未揭开且至少有一个已揭开数字邻居的格子），将边界格子按连通分量分组。对于每个连通分量，若其包含的格子数不超过20个，则采用回溯法穷举所有合法的地雷分配方案，精确计算每个格子的地雷概率。若连通分量过大（超过20个格子），则采用随机采样法近似计算。非边界的内部格子的概率按均匀分布计算。')

add_body(doc, '计算结果在界面上以颜色渐变的形式叠加显示：概率为0的格子显示绿色（确定安全），概率为0.5的格子显示黄色（不确定），概率为1.0的格子显示红色（确定是雷）。玩家可以通过按P键切换热力图的常驻显示，也可以通过按住Alt键临时查看热力图（松开后立即消失），后者提供了类似X光透视的直觉体验。')

add_heading_ch3(doc, '2.3.4 环面拓扑实现')

add_body(doc, '经典扫雷的棋盘有固定边界，边缘格子只有5个邻居，角落格子只有3个邻居。环面模式将棋盘的上下边和左右边分别连通，形成拓扑学中的环面（类似甜甜圈的表面）。在这种模式下，所有格子都拥有完整的8个邻居：最左列格子的左邻居是最右列的格子，最上行格子的上邻居是最下行的格子。')

add_body(doc, '实现方式极为简洁：统一通过一个邻居查找函数获取邻居坐标。在正常模式下，该函数检查坐标是否越界；在环面模式下，该函数将坐标进行取模运算。由于所有涉及邻居计算的函数（BFS洪水填充、求解器、概率计算、布雷安全区检查、数字计算、和弦操作）均调用此统一接口，环面模式的开启对上层逻辑完全透明，无需任何额外修改。')

add_heading_ch2(doc, '2.4 功能模块详细设计')

add_body(doc, '本项目共计划实现35项功能，按类别分为七组：基础游戏功能（7项）、智能算法功能（4项）、创新玩法功能（4项）、视觉与交互功能（8项）、用户系统与数据功能（6项）、局域网对战功能（4项）、彩蛋与趣味功能（2项）。')

add_heading_ch3(doc, '2.4.1 基础游戏功能（F01–F07）')

features_basic = [
    ('F01 经典扫雷玩法', '实现左键揭开格子、右键插旗（三态循环：正常→旗帜→问号→正常）、和弦揭开（左键按住数字格时周围格子显示凹陷效果，松开后若旗帜标记正确则自动揭开剩余格子）三种核心操作。'),
    ('F02 三级预设难度', '初级9×9棋盘含10颗雷、中级16×16棋盘含40颗雷、高级30×16棋盘含99颗雷，通过菜单或快捷键切换。'),
    ('F03 自定义棋盘', '弹出对话框允许用户输入宽度（5至50）、高度（5至50）和地雷数量（1至宽×高-9），输入值自动钳位到合法范围。'),
    ('F04 BFS洪水填充', '点击周围无雷的空格时，自动展开整个安全区域。采用显式队列实现，避免递归栈溢出。'),
    ('F05 首次点击安全保护', '第一次点击时才布雷，保证点击位置及其周围八个格子内不出现地雷，确保首次点击必定打开一片区域。'),
    ('F06 实时计时器与雷数显示', '顶部状态栏左侧显示剩余雷数（总雷数减去已插旗数），右侧显示已用秒数，均以LED风格红色数字呈现在黑色凹陷面板中。'),
    ('F07 笑脸按钮', '位于状态栏正中，随游戏状态变化表情——正常时显示笑脸，和弦按压时显示惊讶，胜利时显示墨镜，失败时显示叉眼。点击笑脸可快速重新开始当前难度的游戏。'),
]
for title, desc in features_basic:
    add_body(doc, f'{title}：{desc}')

add_heading_ch3(doc, '2.4.2 智能算法功能（F08–F11）')

features_algo = [
    ('F08 无猜测模式', '默认开启。布雷后使用约束求解器验证雷区是否可以仅通过逻辑推理完全解开，不可解则重新布雷。玩家可通过菜单关闭此模式以获得传统扫雷体验。'),
    ('F09 智能提示', '按H键或点击工具栏"提示"按钮，求解器分析当前局面，找到一个确定安全的格子并用绿色粗边框高亮显示。若求解器无法找到确定性安全格，则不显示提示。'),
    ('F10 概率热力图', '按P键切换常驻显示，或按住Alt键临时查看。每个未揭开格子上叠加显示其地雷概率值，同时用颜色编码——从绿色（安全）到黄色（不确定）到红色（危险）——直观呈现风险分布。'),
    ('F11 AI自动演示', '菜单点击"AI自动演示"，求解器以每步300毫秒的速度自动推理并操作。每一步揭开或标旗时，推理所依据的数字格会以蓝色边框高亮标注，使观众能够跟随AI的逻辑思路。求解器卡住时自动停止。'),
]
for title, desc in features_algo:
    add_body(doc, f'{title}：{desc}')

add_heading_ch3(doc, '2.4.3 创新玩法功能（F12–F15）')

features_play = [
    ('F12 战争迷雾模式', '按F键切换。开启后，只有已揭开格子周围一定半径范围内的区域可见，超出范围的格子渲染为近乎全黑的迷雾色。该模式大幅增加了游戏难度和探索感。'),
    ('F13 环面模式', '菜单切换。棋盘上下左右边界连通，所有格子均拥有8个邻居。该模式消除了经典扫雷中"角落和边缘格子更安全"的策略，要求玩家全方位思考。'),
    ('F14 限时挑战模式', '设定倒计时（初级60秒、中级180秒、高级300秒），时间耗尽则判负。顶部计时器改为倒数显示，最后10秒变为红色闪烁警示。'),
    ('F15 地雷密度调节', '在自定义棋盘对话框中增加密度滑块，允许按百分比（10%至30%）而非绝对数量设置地雷数。系统自动计算对应的地雷数量。'),
]
for title, desc in features_play:
    add_body(doc, f'{title}：{desc}')

add_heading_ch3(doc, '2.4.4 视觉与交互功能（F16–F23）')

features_visual = [
    ('F16 翻格波纹动画', '点击揭开多格时，格子从点击点以BFS顺序逐个翻开，每15毫秒翻开3至5个格子，形成从中心向外扩散的波纹效果。单格揭开无动画（保持即时响应感）。动画期间阻塞用户输入，防止操作冲突。'),
    ('F17 胜利与失败特效', '踩雷时窗口左右震动三次（每次偏移5像素，间隔40毫秒），随后地雷以每80毫秒一个的速度逐个冒出。胜利时剩余未揭开的格子从棋盘中心向外波纹翻开。'),
    ('F18 四套颜色主题', '经典灰（还原Windows 7外观）、暗黑模式（深色背景减轻视觉疲劳）、海洋蓝（清爽的蓝绿色调）、复古像素（暖色调怀旧风格）。通过菜单一键切换，切换后立即生效。所有颜色通过ColorTheme结构体统一管理，渲染代码不包含任何硬编码颜色值。'),
    ('F19 完整键盘操作', '方向键移动光标（可见的金色脉冲边框），空格键或回车键揭开光标位置的格子，F键插旗，D键和弦，Tab键智能跳转到下一个待决策的格子。光标在使用鼠标时自动隐藏。'),
    ('F20 九宫格按压反馈', '左键按住一个已揭开的数字格时，周围3×3区域内的未揭开、未插旗格子显示凹陷效果。松开左键时自动执行和弦操作——若已插旗数与数字相符则揭开剩余格子，否则恢复原状。该交互完全还原了Windows 7扫雷的操作手感。'),
    ('F21 双缓冲渲染', '所有绘制操作首先在内存画布上完成，绘制完毕后一次性贴到屏幕。此技术彻底消除了频繁重绘导致的画面闪烁，尤其在波纹动画等高频刷新场景下保证画面流畅。'),
    ('F22 Alt概率透视', '按住Alt键时实时计算并显示概率热力图，松开Alt键立即消失。与P键的常驻热力图不同，Alt透视更像是一把"X光手电筒"——按住看一眼，松开继续凭自己判断，保留了游戏的挑战性。'),
    ('F23 色盲友好模式', '提供一套高对比度替代配色方案，将传统的红蓝绿数字颜色替换为色盲用户可区分的颜色组合。该模式可在主题系统中作为额外选项提供。'),
]
for title, desc in features_visual:
    add_body(doc, f'{title}：{desc}')

add_heading_ch3(doc, '2.4.5 用户系统与数据功能（F24–F29）')

features_user = [
    ('F24 用户注册与登录', '程序启动时弹出登录对话框，提供三个按钮——登录（输入用户名和密码验证）、注册（创建新账户，密码使用djb2哈希算法存储）、游客（跳过登录直接开始游戏）。登录成功后窗口标题栏显示用户名。用户数据存储在本地二进制文件中。'),
    ('F25 个人排行榜', '每个难度维护一份Top 10排行榜，按通关用时从短到长排列。每条记录包含用户名、用时和日期。通关后若成绩进入前10，弹出消息提示排名。'),
    ('F26 游戏统计', '记录每个难度的已玩局数、胜利局数和最佳时间。统计数据按用户独立保存，通过菜单"统计"查看。'),
    ('F27 操作录制', '游戏过程中自动记录每一步操作（点击坐标、操作类型、距游戏开始的毫秒时间戳）。录制数据存储在内存中，可通过菜单导出为.msrep二进制回放文件。'),
    ('F28 回放播放', '加载.msrep回放文件后，系统重建原始雷区并按时间戳逐步重现玩家的操作过程。支持1倍速、2倍速和4倍速播放，播放过程中可暂停。'),
    ('F29 成就系统', '预设若干成就目标，例如"初级10秒内通关""连续5局不踩雷""高级难度纯键盘通关"等。达成成就时弹出提示，已获得的成就在统计界面中展示。'),
]
for title, desc in features_user:
    add_body(doc, f'{title}：{desc}')

add_heading_ch3(doc, '2.4.6 局域网对战功能（F30–F33）')

features_net = [
    ('F30 创建与加入房间', '一名玩家通过菜单"联机→创建房间"开启服务端，系统自动显示本机IP地址。另一名玩家通过"联机→加入房间"输入对方IP地址连接。连接建立后双方收到提示。'),
    ('F31 同板竞速', '服务端生成雷区后将地雷位置通过TCP发送给客户端。双方在完全相同的雷区上各自独立扫雷，先揭开所有安全格子的一方获胜。'),
    ('F32 对手进度条', '游戏界面底部显示一条进度条，实时反映对手已揭开的格子数占总安全格子数的比例。通过50毫秒间隔的轮询定时器接收对方的状态更新。'),
    ('F33 断线处理', '网络异常时自动检测连接中断并弹出提示，玩家可继续单人游戏或重新连接。'),
]
for title, desc in features_net:
    add_body(doc, f'{title}：{desc}')

add_heading_ch3(doc, '2.4.7 彩蛋与趣味功能（F34–F35）')

features_easter = [
    ('F34 Konami Code彩蛋', '在游戏中依次输入经典的上上下下左右左右BA按键序列，系统自动标出所有地雷的位置并播放胜利音效。这一经典游戏彩蛋的实现展示了键盘输入的有限状态机设计。'),
    ('F35 自定义布雷', '提供关卡编辑模式，玩家可手动在空棋盘上放置地雷，保存为自定义关卡文件后分享给其他玩家挑战。该功能既增加了游戏的社交性，也为测试特殊雷区布局提供了便利。'),
]
for title, desc in features_easter:
    add_body(doc, f'{title}：{desc}')

add_heading_ch2(doc, '2.5 界面设计')

add_body(doc, '主界面由四个区域组成。顶部是56像素高的状态栏，左侧为LED风格的雷数显示面板，正中是笑脸按钮，右侧是LED风格的计时器显示面板。中部是雷区网格，每个格子30×30像素，外围有10像素宽的3D凹陷边框。底部是36像素高的工具栏，均匀排列四个按钮：提示(H)、热力图(P)、雾模式(F)、新游戏(N)。菜单栏包含五个下拉菜单：游戏、功能、联机、主题、帮助。程序启动时首先显示登录对话框（用户名输入框、密码输入框、登录/注册/游客三个按钮），登录或选择游客后进入主界面。')

add_body(doc, '格子的视觉设计忠实还原Windows 7扫雷风格：未揭开的格子具有2像素白色高光和1像素深灰阴影的3D凸起效果；已揭开的格子为浅灰色平面，四周有1像素细边框；数字1至8分别使用蓝、绿、红、深蓝、深红、青、黑、灰八种经典颜色显示；旗帜由红色三角形、黑色旗杆和底座组成；地雷为黑色圆形加四条十字交叉刺突和白色高光点。')

add_heading_ch2(doc, '2.6 文件结构设计')

add_body(doc, '项目源代码按照功能职责组织为三级目录结构。include目录存放全部10个头文件，定义各模块的公共接口。src目录存放全部10个源文件，包含具体实现代码。test目录存放自动化测试代码。根目录包含README项目说明、build.bat编译脚本和.gitignore版本管理配置。编译产物（.o目标文件、.exe可执行文件）和运行时数据文件（.dat统计数据、.msrep回放文件）均在.gitignore中排除，不纳入版本管理。')

doc.add_page_break()

# ============================================================
# Chapter 3
# ============================================================
add_heading_ch1(doc, '第三章 系统测试与验证')

add_heading_ch2(doc, '3.1 自动化单元测试')

add_body(doc, '项目编写独立的自动化测试程序test_core.c，覆盖10项核心逻辑测试用例。每项测试独立执行，输出PASS或FAIL结果，最终汇总通过率。')

tests = [
    ('T01 棋盘创建与销毁测试', '验证board_create函数返回非空指针、棋盘宽度高度雷数正确、初始状态为READY、已揭开数和已插旗数为0。调用board_destroy后确认内存正确释放。'),
    ('T02 安全区布雷测试', '调用board_place_mines后，检查首次点击位置及其周围8个邻居格子内不存在地雷；同时验证棋盘上的地雷总数等于指定的地雷数量。'),
    ('T03 BFS洪水填充测试', '在已布雷的棋盘上揭开首次点击位置，验证返回值为0（未踩雷）、已揭开格子数大于0、起始格子状态已标记为REVEALED。'),
    ('T04 插旗三态循环测试', '对同一格子连续执行三次toggle_flag操作，验证状态依次变为FLAGGED、QUESTION、无标记，且flagged_count正确递增和递减。'),
    ('T05 胜利检测测试', '手动设置一个3×3棋盘（仅1颗雷），揭开所有非雷格子后调用check_win，验证返回值为1且状态变为WON。'),
    ('T06 求解器正确性测试', '在已布雷棋盘上运行求解器，验证求解器不会触发地雷，且求解前后原始棋盘状态不变（求解器在副本上操作）。'),
    ('T07 无猜测生成验证测试', '连续生成20局初级难度棋盘，对每局使用求解器完整求解，验证至少75%的棋盘可以纯逻辑通关。'),
    ('T08 提示安全性测试', '在无猜测棋盘上揭开起始区域后请求提示，验证提示返回的格子确实不是地雷。'),
    ('T09 概率范围测试', '在已部分揭开的棋盘上计算概率热力图，验证所有已揭开格子的概率值为-1.0，所有未揭开格子的概率值在0.0至1.0的合法范围内。'),
    ('T10 难度参数测试', '调用game_get_dimensions验证三级难度的参数——初级9×9含10雷、中级16×16含40雷、高级30×16含99雷。'),
]
for title, desc in tests:
    add_body(doc, f'{title}：{desc}')

add_heading_ch2(doc, '3.2 手动集成测试')

add_body(doc, '除自动化测试外，项目还制定以下手动测试方案验证各项交互功能。')

manual_tests = [
    '波纹动画测试：在初级难度下点击空白区域，观察格子是否从点击点以波纹形式逐个翻开，而非瞬间全部出现。',
    '主题切换测试：通过菜单依次切换四套主题，确认每套主题的所有颜色均正确渲染，无花屏或颜色错位。',
    '键盘操作测试：全程不使用鼠标，仅通过方向键、空格键和F键完成一局初级难度游戏。',
    '踩雷特效测试：故意踩雷后观察窗口是否发生左右震动，地雷是否逐个冒出而非全部瞬间显现。',
    'AI演示测试：在游戏进行中点击AI自动演示，观察求解器是否逐步揭开格子，推理依据格是否有蓝色高亮。',
    '环面模式测试：开启环面模式，在棋盘边缘点击并观察BFS展开是否正确穿越边界。',
    'Alt透视测试：游戏中按住Alt键，确认概率热力图立即出现；松开Alt键，确认热力图立即消失。',
    '联机对战测试：在局域网两台电脑上分别启动程序，一方创建房间，另一方输入IP加入，验证双方在相同雷区上竞速且对手进度条实时更新。',
    'Konami彩蛋测试：在游戏中依次按下上上下下左右左右BA，验证所有地雷位置被自动标旗。',
]
for t in manual_tests:
    add_body(doc, t)

add_heading_ch2(doc, '3.3 性能指标')

add_body(doc, '编译质量方面，程序在gcc的-Wall -Wextra编译选项下零警告零错误。可执行文件大小控制在200KB以内。程序启动时间（从双击到主界面显示）不超过1秒。渲染帧率通过双缓冲技术保证60FPS无闪烁。无猜测雷区生成的成功率在初级难度下超过95%、中级超过80%、高级超过50%。自动化单元测试通过率为10项全部通过。')

doc.add_page_break()

# ============================================================
# Chapter 4
# ============================================================
add_heading_ch1(doc, '第四章 项目管理与总结')

add_heading_ch2(doc, '4.1 团队分工')

add_body(doc, '项目团队由四名成员组成，按照模块职责明确分工。成员A担任组长，负责main.c和game.c模块的开发（游戏核心循环、状态机、所有对话框、菜单系统、定时器调度、功能集成），同时负责项目管理、进度协调和系统设计文档的撰写。成员B负责算法模块board.c和solver.c的开发（棋盘数据结构、布雷算法、BFS洪水填充、约束求解器、概率计算、环面拓扑支持），以及自动化测试test_core.c的编写。成员C负责表现层模块render.c和sound.c的开发（Win32 GDI渲染管线、双缓冲、主题系统、动画效果、光标绘制、音效播放），以及PPT汇报演示文稿的制作。成员D负责功能层模块replay.c、stats.c、user.c和net.c的开发（操作录制与回放、排行榜与统计、用户登录注册、Winsock局域网对战）。')

add_heading_ch2(doc, '4.2 开发进度计划')

add_body(doc, '项目开发周期为7周，分为六个阶段。')

schedule = [
    ('第一阶段（第1至2周）', '完成核心玩法：窗口创建、棋盘数据结构、随机布雷、BFS洪水填充、基础GDI渲染、鼠标交互。目标是实现一个可以点击揭开格子的基础版本。'),
    ('第二阶段（第3周）', '完成完整游戏：三级难度切换、游戏状态机、插旗与和弦操作、计时器与LED雷数显示、笑脸按钮。目标是实现一个功能完整的经典扫雷。'),
    ('第三阶段（第4周）', '完成智能算法：求解器三规则实现、无猜测雷区生成、提示系统、概率热力图计算与渲染。目标是将算法创新功能全部上线。'),
    ('第四阶段（第5周）', '完成创新功能：主题系统、键盘导航、翻格波纹动画、胜负特效、环面模式、AI自动演示、Alt透视、Konami彩蛋。目标是将所有视觉与交互创新功能集成到位。'),
    ('第五阶段（第6周）', '完成社交功能：用户登录注册、排行榜与统计、操作录制与回放播放、局域网双人对战。目标是将用户系统与联机功能全部打通。'),
    ('第六阶段（第7周）', '打磨与测试：运行自动化测试、修复Bug、性能优化、撰写课程实践报告、制作PPT。目标是交付一个经过充分测试的完整产品。'),
]
for title, desc in schedule:
    add_body(doc, f'{title}：{desc}')

add_heading_ch2(doc, '4.3 技术难点与应对方案')

add_body(doc, '本项目在开发过程中预计将面临以下技术难点。')

difficulties = [
    'GDI频繁绘图导致的画面闪烁问题，通过双缓冲技术解决——所有绘制先完成在内存画布上，再一次性贴到屏幕。',
    'BFS递归可能导致的栈溢出问题，通过使用显式队列（malloc动态数组）替代函数递归调用来解决。',
    '求解器在高级难度下的效率问题，通过三规则迭代剪枝减少不必要的搜索，大型连通分量的概率计算改用随机采样近似。',
    '中文字符在不同系统上的显示兼容性问题，通过全部使用Unicode宽字符配合Win32的W后缀API来解决，使程序不依赖源文件的编码格式。',
    'Winsock网络编程的阻塞问题，通过在连接建立后将套接字设为非阻塞模式，配合select函数和定时器轮询来解决。',
    '音效播放阻塞UI线程的问题，通过CreateThread创建后台线程执行Beep调用，主线程立即返回不等待。',
]
for d in difficulties:
    add_body(doc, d)

add_heading_ch2(doc, '4.4 项目特色与创新点总结')

add_body(doc, '本项目在以下六个方面体现了超越常规课程作业的创新深度。')

innovations = [
    '一个算法引擎驱动三个功能：约束求解器同时支撑无猜测模式、智能提示和AI自动演示，展示了算法复用与架构设计的统一性。',
    '环面拓扑模式仅需修改邻居坐标计算（取模运算），即可将经典扫雷转变为在拓扑学环面上进行的数学游戏，体现了用最少的代码改动实现最大的玩法创新。',
    '翻格波纹动画通过将BFS结果缓存为动画队列并由定时器驱动逐帧揭开，在纯C语言Win32 GDI环境下实现了流畅的视觉效果。',
    '主题系统通过将所有颜色值封装进结构体数组，实现了渲染代码与颜色数据的完全解耦，新增主题只需添加一组颜色数据。',
    '完整的键盘无障碍操作展示了对用户体验包容性的设计思考，Tab智能跳转功能通过扫描邻近数字格的未揭开邻居实现高效导航。',
    'Konami Code彩蛋用10元素状态机阵列实现了经典游戏文化的致敬，代码仅25行却涵盖了有限状态自动机的设计思想。',
]
for inv in innovations:
    add_body(doc, inv)

doc.add_page_break()

# ============================================================
# References
# ============================================================
add_heading_ch1(doc, '参考资料')

refs = [
    '[1] Microsoft. Win32 API Documentation — Windows GDI. docs.microsoft.com',
    '[2] Petzold C. Programming Windows, 5th Edition. Microsoft Press, 1998',
    '[3] Becerra D J. Algorithmic Approaches to Playing Minesweeper. Bachelor Thesis, Harvard University, 2015',
    '[4] Kaye R. Minesweeper is NP-complete. The Mathematical Intelligencer, 22(2): 9-15, 2000',
    '[5] 谭浩强. C程序设计（第五版）. 北京: 清华大学出版社, 2017',
    '[6] Stevens W R. UNIX Network Programming, Volume 1: The Sockets Networking API, 3rd Edition. Addison-Wesley, 2003',
]
for ref in refs:
    add_body(doc, ref, indent=False)

# ============================================================
# Save
# ============================================================
output_path = r'F:\C\minesweeper\docs\项目计划书.docx'
doc.save(output_path)
print(f'OK: {output_path}')
print(f'Size: {os.path.getsize(output_path)} bytes')
